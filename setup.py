from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Ad click prediction demo case',
    author='Robert van Straalen',
    license='',
)
